[← Back to Root](../README.md) / **Frontend**
---
# Portfolio Frontend (React + Vite)

This is the source code for the portfolio website.

## Development
To run this project locally inside **app** folder:
1. `npm install`
2. `npm run dev`

## Build
The project builds to the `dist/` folder via: 
`npm run build`
