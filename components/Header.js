import Navbar from '../components/Navbar'

function Header() {
    return (
        <header className="fixed top-0 w-full bg-white z-50 shadow-sm dark:bg-gray-700">
            <Navbar />
        </header>
    )
}

export default Header
